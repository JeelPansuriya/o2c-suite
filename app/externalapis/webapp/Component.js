sap.ui.define([
    "sap/ui/core/UIComponent"
], function (UIComponent) {
    "use strict";

    return UIComponent.extend("o2c.externalapis.Component", {
        metadata: {
            manifest: "json"
        }
    });
});
